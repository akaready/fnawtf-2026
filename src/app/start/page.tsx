import { IntakeFormClient } from './_components/IntakeFormClient';

export const metadata = {
  title: 'FNA.wtf • Start a Project',
  description: 'Tell us about your project and let\'s create something great together.',
};

export default function StartPage() {
  return <IntakeFormClient />;
}
